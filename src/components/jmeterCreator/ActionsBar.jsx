// Este componente ya no es necesario, el botón está integrado en TabNavigation
// Se puede eliminar este archivo o mantenerlo comentado para referencia futura

/*
import React from 'react';
import { Button, ButtonGroup } from 'react-bootstrap';

const ActionsBar = ({ onOpenPreview }) => {
    return (
        <ButtonGroup className="mb-3">
            <Button size="sm" variant="primary" onClick={onOpenPreview}>
                Previsualizar .jmx
            </Button>
        </ButtonGroup>
    );
};

export default ActionsBar;
*/
