import React from 'react';
import ServersRequest from '@/components/serversRequest';

const ServersRequestPage = () => {
    return (
        <ServersRequest />
    );
};

export default ServersRequestPage;
